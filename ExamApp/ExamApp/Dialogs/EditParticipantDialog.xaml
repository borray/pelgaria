<Window x:Class="ExamApp.Dialogs.EditParticipantDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Участник" Height="360" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Фамилия *" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                 Padding="8,6" FontSize="13" Margin="0,0,0,10" BorderBrush="#BDBDBD" BorderThickness="1"/>

        <TextBlock Grid.Row="2" Text="Имя *" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                 Padding="8,6" FontSize="13" Margin="0,0,0,10" BorderBrush="#BDBDBD" BorderThickness="1"/>

        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                <TextBlock Text="Отчество" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding MiddleName, UpdateSourceTrigger=PropertyChanged}"
                         Padding="8,6" FontSize="13" BorderBrush="#BDBDBD" BorderThickness="1"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Группа *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}"
                         Padding="8,6" FontSize="13" BorderBrush="#BDBDBD" BorderThickness="1"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="5" Text="Email" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox Grid.Row="6" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Padding="8,6" FontSize="13" BorderBrush="#BDBDBD" BorderThickness="1"
                 VerticalAlignment="Top"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Отмена" Width="90" Height="34" Margin="0,0,10,0"
                    Click="CancelButton_Click"
                    Background="White" BorderBrush="#BDBDBD" BorderThickness="1"/>
            <Button Content="Сохранить" Width="110" Height="34"
                    Click="SaveButton_Click"
                    Background="#1565C0" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
